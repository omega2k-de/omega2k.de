{
  "name": "websocket-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "implicitDependencies": ["websocket"],
  "targets": {
    "e2e": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{e2eProjectRoot}"],
      "options": {
        "jestConfig": "apps/websocket-e2e/jest.config.ts"
      },
      "dependsOn": ["websocket:build", "websocket:serve"]
    }
  }
}
